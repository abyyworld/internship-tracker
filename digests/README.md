# Discovery history

One file per day, written by `internship_watcher.py`, listing the roles that
were first seen that morning. Generated output: nothing reads these files, and
editing one by hand only means the next run disagrees with it.

They are kept because a dated record of when a posting appeared is the only way
to tell a genuinely new opening from one a source simply started reporting.
